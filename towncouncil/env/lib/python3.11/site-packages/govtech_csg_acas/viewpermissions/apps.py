from django.apps import AppConfig
from .patch.patch import Patcher


class AcasAppConfig(AppConfig):
    default_auto_field = 'django.db.models.AutoField'
    name = 'govtech_csg_acas.viewpermissions'

    def ready(self):
        Patcher.do_patch()